<nav id="form-navigation" class="govuk-navigation-app" aria-label="Form menu">
  <div class="govuk-width-container">
    <h2 id="form-navigation-heading" class="service-name heading govuk-heading-s">
      <%= service.service_name %>
    </h2>

    <ul class="govuk-navigation">
      <li class="<%= controller_name == 'services' ? 'current' : '' %>">
        <%= link_to t('pages.name'), pages_url %>
      </li>

      <li class="<%= controller_name == 'settings' ? 'current' : '' %>">
        <%= link_to t('settings.name'), settings_path(service.service_id) %>
      </li>

      <li class="<%= controller_name == 'publish' ? 'current' : '' %>">
        <%= link_to t('publish.name'), publish_index_path(service.service_id) %>
      </li>
    </ul>
  </div>
</nav>
