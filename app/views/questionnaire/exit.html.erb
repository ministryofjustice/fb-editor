<%= link_to t("actions.back"), questionnaire_path(:continue), class: "govuk-back-link" %>

<%= render 'page_layout' do %>
  <p class="govuk-body"><%= t("questionnaire.#{@page}.subheading").html_safe %></p>
  <%= link_to t('dialogs.link_back_to_your_forms'), back_to_services_questionnaire_index_path %>
<% end %>
