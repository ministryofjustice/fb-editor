apiVersion: v1
kind: Secret
metadata:
  name: <%= secret_name %>
  namespace: <%= namespace %>
data: <% (secrets << ms_graph_api_secrets).flatten.each do |secret|  %>
  <%= secret.name.downcase %>: "<%= secret.encode64 %>"
<% end %>
